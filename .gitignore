__pycache__/
*.pyc
.venv/
venv/

# Ortam / secrets — gerçek API anahtarları asla commit'lenmez
.env
.env.local
code/.env
code/.env.local

# OS junk
.DS_Store

# Runtime SQLite DB (backend §5) — commit edilmez
code/data/runtime/
