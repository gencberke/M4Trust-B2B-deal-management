import{beforeEach,afterEach,describe,expect,it,vi}from"vitest";import{resetApiClientForTests}from"./client";import{listPaymentResolutions,reconcilePayments}from"./payments";describe("payments api",()=>{beforeEach(()=>Object.defineProperty(globalThis,"document",{configurable:true,value:{cookie:"m4t_csrf=x"}}));afterEach(()=>{resetApiClientForTests();vi.unstubAllGlobals();Reflect.deleteProperty(globalThis,"document")});it("reads resolutions and reconciles without optimistic state",async()=>{const f=vi.fn().mockImplementation(()=>Promise.resolve(new Response(JSON.stringify({transaction_id:"t",resolutions:[],results:[]}),{status:200,headers:{"content-type":"application/json"}})));vi.stubGlobal("fetch",f);expect((await listPaymentResolutions("t")).resolutions).toEqual([]);expect((await reconcilePayments("t")).results).toEqual([])})});
