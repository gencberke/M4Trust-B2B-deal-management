import{apiRequest}from"./client";import type{ExecuteResolutionResponse,PaymentResolution,PaymentResolutionList,PaymentTraceResponse,ReconcileResponse,ReleaseRetryResponse}from"../types/payments";const post=<T>(p:string)=>apiRequest<T>(p,{method:"POST",body:{},csrf:true,redirectOnError:false});export const reconcilePayments=(id:string)=>post<ReconcileResponse>(`/transactions/${encodeURIComponent(id)}/payments/reconcile`);export const retryReleaseInstruction=(id:string)=>post<ReleaseRetryResponse>(`/release-instructions/${encodeURIComponent(id)}/retry`);export const requestUndo=(id:string)=>post<PaymentResolution>(`/funding-units/${encodeURIComponent(id)}/undo-request`);export const requestRefund=(id:string)=>post<PaymentResolution>(`/funding-units/${encodeURIComponent(id)}/refund-request`);export const approveResolution=(id:string)=>post<PaymentResolution>(`/payment-resolutions/${encodeURIComponent(id)}/approvals`);export const executeResolution=(id:string)=>post<ExecuteResolutionResponse>(`/payment-resolutions/${encodeURIComponent(id)}/execute`);export const listPaymentResolutions=(id:string)=>apiRequest<PaymentResolutionList>(`/transactions/${encodeURIComponent(id)}/payment-resolutions`,{redirectOnError:false});export const getPaymentTrace=(id:string)=>apiRequest<PaymentTraceResponse>(`/transactions/${encodeURIComponent(id)}/payment-trace`,{redirectOnError:false});
