import{ResponsiveTable}from"../../../components/ResponsiveTable";import{formatDateTime,shortId}from"../../../lib/format";import type{EvidenceRecordSummary}from"../../../types/evidence";
export function EvidenceRecordsPanel({records}:{records:EvidenceRecordSummary[]}){return <ResponsiveTable caption="Kanıt kayıtları" head={["Tür","Durum","Referans","Aşama","Gönderen","Tarih"]} emptyLabel="Kanıt kaydı yok" rows={records.map(r=>({key:r.id,cells:[r.evidence_type??"—",r.verification_status??"—",r.external_reference??(r.file_sha256?shortId(r.file_sha256,12):"—"),r.milestone_id?shortId(r.milestone_id):"—",r.submitted_by_role??"—",formatDateTime(r.created_at)]}))}/>}
