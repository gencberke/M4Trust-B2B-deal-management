import{describe,expect,it}from"vitest";import{executeConfirmWord,outcomeTone,paymentErrorMessage}from"./paymentsLogic";describe("payment logic",()=>{it("unknown is warning",()=>expect(outcomeTone("unknown")).toBe("warning"));it("uses typed words",()=>{expect(executeConfirmWord("refund")).toBe("IADE");expect(executeConfirmWord("undo_approval")).toBe("GERI-AL")});it("maps errors",()=>expect(paymentErrorMessage("PAYMENT_RETRY_CONFLICT")).toContain("Talimat"))});
