import{buttonClass}from"../../shared";import{Notice}from"../../../components/Feedback";import type{ReconcileResult}from"../../../types/payments";export function ReconcilePanel({busy,results,onRun}:{busy:boolean;results:ReconcileResult[]|null;onRun:()=>void}){return <div className="space-y-3"><button className={buttonClass} disabled={busy} onClick={onRun}>Ödemeleri mutabakat et</button>{results?<Notice tone="info">{results.length?`${results.length} birim backend tarafından mutabakat edildi.`:"Mutabakat bekleyen birim yok."}</Notice>:null}</div>}
