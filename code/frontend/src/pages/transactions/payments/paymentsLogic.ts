export const executeConfirmWord=(op:string)=>op==="refund"?"IADE":"GERI-AL";export function outcomeTone(s:string):"success"|"warning"|"danger"|"neutral"{if(s==="unknown"||s.includes("unknown"))return"warning";if(["confirmed","executed","success","approved"].includes(s))return"success";if(["failed","rejected"].includes(s))return"danger";return"neutral"}export function paymentErrorMessage(code:string){const m:Record<string,string>={PAYMENT_OPERATION_FORBIDDEN:"Bu ödeme operasyonu için yetkiniz yok.",PAYMENT_RECONCILE_CONFLICT:"Mutabakat çakıştı; durumu yenileyin.",RELEASE_INSTRUCTION_NOT_FOUND:"Serbest bırakma talimatı bulunamadı.",PAYMENT_RETRY_CONFLICT:"Talimat güvenli biçimde tekrar çalıştırılamadı.",PAYMENT_RESOLUTION_CONFLICT:"Çözüm talebi oluşturulamadı.",PAYMENT_RESOLUTION_APPROVAL_FORBIDDEN:"Bu taraf adına onay yetkiniz yok.",PAYMENT_RESOLUTION_EXECUTION_CONFLICT:"Çözüm uygulanamadı; güncel onayları yenileyin.",PAYMENT_TRACE_LEGACY_UNSUPPORTED:"Bu işlem için ödeme izi yok."};return m[code]??`Ödeme işlemi tamamlanamadı (${code}).`}
