import{describe,expect,it}from"vitest";import{DISPUTE_ACTIONS,disputeErrorMessage,safeDisputeAction}from"./disputesLogic";describe("disputes logic",()=>{it("freezes live actions",()=>expect(DISPUTE_ACTIONS).toEqual(["comment","attach_evidence","escalate_dispute","resolve","cancel"]));it("maps conflict",()=>expect(disputeErrorMessage("DISPUTE_CLOSED")).toContain("kapalı"));it("drops tokens",()=>expect(safeDisputeAction({action:"comment",comment:"x",token:"s"})).toEqual({action:"comment",comment:"x"}))});
