# AGENTS.md — M4Trust

Bu repo, **M4Trust** projesidir: Moka United Fintech Hackathon için geliştirilen, AI destekli bir B2B **şartlı ödeme / güven katmanı**. Sözleşme okunur → kurallar çıkarılır → deterministik validator denetler → iki taraf onaylar → teslimat kanıtına göre mock ödeme kararı üretilir.

## Önce bunu oku

Proje hakkında bağlamın yoksa, herhangi bir işe başlamadan önce şu sırayla oku:

1. **[ARCHITECTURE.md](ARCHITECTURE.md)** — bağlayıcı teknik referans: genel mimari, model/servis iletişimi (adapter'lar), API contract'ları, tech stack ve dışına çıkılmayacak tasarım kalıpları. **Koda dokunan her iş bu dokümana uymak zorundadır.**
2. **[plans/](plans/)** — planlama dokümanları (bkz. [plans/README.md](plans/README.md)): durum = klasör — `planning/` üzerinde çalışılan taslaklar, `ready/` uygulanmaya hazır planlar, `done/` uygulaması tamamlanmış planlar. 
3. **[report/](report/)** — sözel bağlam PDF'leri: [ana yapı ve kararlar](report/ "md dosyaları") (gerekçeler, jüri savunmaları), [kısa proje raporu](report/ "md dosyaları") (fikir anlatımı).

Akış diyagramları `diagram/` klasöründedir.

## Repo düzeni

```
ARCHITECTURE.md   bağlayıcı teknik referans  ← ÖNCE BUNU OKU
YOL_HARITASI.md   hackathon geliştirme yol haritası (sürekli referans)
plans/planning/   üzerinde çalışılan taslaklar (öneri tonunda, bağlayıcı değil)
plans/ready/      uygulanmaya hazır planlar — uygulama /plan-uygula komutuyla
plans/done/       uygulaması tamamlanmış planlar (tarihçe + sapma kaydı)
report/           proje raporları (sözel bağlam, PDF)
diagram/          akış diyagramları (PNG)
code/scripts/     offline RAG hazırlığı (chunk + embed — çalışır durumda)
code/data/        mevzuat korpusu (Chroma index hazır) + 31 ham sözleşme PDF'i
code/backend/     FastAPI servisi (kuruldu: app + db + eventbus + router'lar + servisler)
code/frontend/    React + Vite + Tailwind (ARCHITECTURE §1'e göre kurulacak)
```

## Değişmez ilkeler (tam liste: ARCHITECTURE.md §6)

- **LLM asla ödeme kararı vermez.** Zincir: LLM önerir → validator (deterministik) denetler → insanlar onaylar → motor uygular.
- **Gerçek para hareketi ve gerçek kart verisi yok.** Ödeme, Moka havuz ödeme contract'ına uygun mock provider ile simüle edilir.
- **Her dış bağımlılık adapter + fake çiftidir; dış LLM'e yalnızca maskelenmiş içerik gider.**
- **Extraction JSON şeması ikili sözleşmedir** (ARCHITECTURE §4.2): değiştirmeden önce ekipçe mutabakat gerekir. Platformun operasyonel takip tercihi bu şemaya yazılmaz.
- **Video tek başına para hareketi üretemez.** Opsiyonel video advisory'dir: teslim miktarını, kısmi ödeme oranını, release'i veya dispute'u belirleyemez; en fazla `hold` + manuel inceleme tetikler.
- **Takip politikası taraf onaylarından önce kilitlenir**; sözleşmesel kanıt şartı (`required_evidence`) yönetici tercihiyle kapatılamaz veya zayıflatılamaz (sözleşmesel video ⇒ `document_and_video` zorunlu).
- **Release guard tek yerdedir** (`services/settlement.py`); `decision.py` ve `milestone_decision.py` saf fonksiyon kalır (evaluator settlement'a gömülmez), router'lar ödeme mantığının sahibi değildir ve provider çağırmaz.
- **Funding-unit modeli (Plan 06): bir funding unit = bir pool payment = tek bütün approve.** `capture_ratio` account release yolundan kalktı; funding exactly-once; provider `unknown` sonucu definitive failure sayılmaz (reconcile-first, kör create/approve retry yok). Legacy `MockMokaProvider`/`mock_payments` yolu `legacy_v1` için korunur.
- **Capability token'ları (buyer/seller/manager) log, event ve evidence'a girmez.** `LEGACY_CAPABILITY_ACCESS_ENABLED` varsayılanı Plan 06 closure'dan sonra **false**'tur (env ile açılır).
- **Sözleşme alıntısı (`source_quote`) yalnızca token'lı uçlarda ve maskelenmiş olarak döner.** Maskeleme NER değildir; token istemeyen uçlar alıntıyı hiç görmez.
- Dokümantasyon ve UI dili **Türkçe**dir.

## Plan yaşam döngüsü ve doc-sync protokolü

Plan durumu klasörle ifade edilir: `plans/planning/` (taslak) → `plans/ready/` (uygulanmaya hazır) → `plans/done/` (uygulandı). Bir planın uygulanması tercihen `/plan-uygula <plan-dosyası>` komutuyla yapılır; komut kullanılmasa bile **koda dokunan her iş için** şu protokol geçerlidir:

1. **Önce çelişki kontrolü:** Yapacağın iş ARCHITECTURE.md ile çelişiyor mu? §6'daki değişmez kalıplardan birini deliyorsa durup kullanıcıya sor. Mimaride henüz tanımlı olmayan bir yenilik (endpoint, servis, bağımlılık, event, şema alanı) getiriyorsa not al.
2. **Sonra doc-sync (işin parçası, atlanamaz):** Değişikliğin eskittiği dokümanı güncelle — endpoint → ARCHITECTURE §4.1 · extraction şeması → §4.2 (mutabakat şart) · event → §4.3 · servis/modül → §1 dizin · bağımlılık → §2 stack · tablo/state → §5 · yeni değişmez kural → §6 + buradaki özet · korpus/pratik gerçekler → buradaki "Pratik notlar". Güncelleme gerekmiyorsa raporunda "doc-sync: değişiklik gerekmedi" de.
3. **Plan durum bloğu:** Uyguladığın planın en üstüne `> **Durum:** Uygulandı — YYYY-AA-GG · Sapmalar: …` işle ve dosyayı `plans/done/` altına taşı.

Dokümantasyonu eskiten bir iş, doc-sync yapılmadan "bitti" sayılmaz.

## Pratik notlar

- Chroma index: `code/data/processed/embeddings/chroma/` — iki embed'li koleksiyon: `legal_articles` (891 vektör: TBK, 6493, 5549, KVKK, Yönetmelik, Tebliğ) ve `contract_examples` (395 vektör, 31 örnek sözleşme, few-shot yapısal referans). Üçüncü koleksiyon **`security_controls`** (PCI DSS kontrol haritası) için chunk'lar hazır (`data/processed/chunks/security/pci_dss_control_map.json`, 6 kontrol + 1 intro) ama **henüz embed edilmedi** (ağır RAG deps yoktu; `build_rag.py` çalıştırıldığında oluşur). Sorgular da BGE-M3 ile encode edilmelidir.
- İki TCMB metni (Yönetmelik + Tebliğ) `code/data/raw/legal/` altına eklendi, chunk'landı ve embed edildi — bkz. [plans/ready/regulasyon_rag_genisletmesi.md](plans/done/regulasyon_rag_genisletmesi.md).
- `code/scripts/convert_documents.py` dolduruldu (`code/scripts/document_parser/`: PyMuPDF/python-docx/Tesseract, Clean Architecture, testli). İş sıralaması için [YOL_HARITASI.md](YOL_HARITASI.md).
- **AI extraction hattı uygulandı** (2026-07-08, [plans/done/ai_extraction_hatti.md](plans/done/ai_extraction_hatti.md)): `code/backend/app/` altında `config.py`, `schemas/extraction.py` (§4.2 donmuş Pydantic ikili sözleşme), `services/rag.py` (BGE-M3+Chroma, lazy), `services/privacy.py` (minimal PII mask/restore, §6.7), `services/extraction.py` (Fake + canlı OpenAI-uyumlu). `scripts/extract_contract.py` artık dolu (CLI: convert→mask→retrieve→extract→restore). LLM `gpt-5.4-mini`, `LLM_PROVIDER=fake|openai` (default `fake`); canlı çağrı için `code/backend/.env.example`'ı `code/.env`'e kopyalayıp `LLM_*` doldur.
- **RAG ContextBuilder + kart-verisi guardrail uygulandı** (2026-07-09, [plans/done/rag_context_builder_ve_guvenlik_katmani.md](plans/done/rag_context_builder_ve_guvenlik_katmani.md)): `services/context_builder.py` (çoklu-query/çoklu-koleksiyon orkestrasyon → `ContextPack`, §3.2); `ExtractionService.extract()` imzası `context: ContextPack | None`'a geçti (§3.1); `services/privacy.py`'ye `analyze()/PrivacyReport` (PAN+Luhn/CVV/track/PIN, kart placeholder'ı restore edilmez, SAD → blocking, §3.5); `config.security_collection`; `build_rag.py` `security/` → `security_controls` dalı; `scripts/extract_contract.py` CLI: convert→**analyze**→ContextBuilder→extract→restore + blocking'de canlı LLM atlanır + "dayanaklar" özeti (stderr). `--collection` **deprecated** (verilirse tek-koleksiyon debug bypass). §4.2 şeması **değişmedi**.
- **Backend iskeleti + işlem akışı uygulandı** (2026-07-09, [plans/done/backend_iskeleti_ve_islem_akisi.md](plans/done/backend_iskeleti_ve_islem_akisi.md)): `code/backend/app/` altında `main.py` (app factory + startup `init_db` + `/health`), `db.py` (stdlib sqlite3, WAL, §5'teki 6 tablo, `connect/init_db/get_db`), `eventbus.py` (`emit` → events, §4.3). Servisler: `validator.py` (saf, 9 kontrol, REJECT>NEEDS_REVIEW>PASS), `decision.py` (saf, capture/partial/hold/dispute), `payment_provider.py` (`PaymentProvider` ABC + `MockMokaProvider`, gerçek Moka response şekli + `mock_payments` ledger), `video.py` (`VideoAnalyzer` ABC + `FakeVideoAnalyzer`, dosya-adı ipuçları `hasarli`/`eksik`), `evidence.py` (`build_bundle`, ham PII/kart/markdown içermez). Router'lar (§4.1'in 8 endpoint'i, ek yok): `transactions` (upload→arka plan pipeline: convert→analyze→ContextBuilder→extract→restore→validate; ham markdown yalnızca DB'de), `approvals` (token'lı idempotent çift onay → `create_pool_payment` → active), `delivery` (e-irsaliye + video → `decide` → §6.1 guard'ında release), `evidence`. §4.2 şeması **değişmedi**. **Çalıştırma:** `cd code && ./.venv/bin/python -m uvicorn backend.app.main:app --reload` (→ `/docs`); tek worker + WAL (çoklu worker çalıştırılmaz). Runtime DB: `code/data/runtime/m4trust.db` (gitignore). `document_parser`, `code/scripts` paketinden normal `scripts.document_parser` importuyla yüklenir; test-only `sys.path` köprüsü gerekmez.
- **Env:** `code/.venv` (Python 3.12) — testler `cd code && ./.venv/bin/python -m pytest` (149 test yeşil). Minimal + web bağımlılıkları kurulu (pydantic, openai, parser, fastapi/uvicorn/python-multipart/httpx). **Canlı RAG için** `pip install -r requirements.txt` gerekir (chromadb + FlagEmbedding/torch, ~GB'lar); kurulu değilse pipeline RAG'ı atlayıp bağlamsız devam eder (graceful degradation). API testleri `with TestClient(app) as c:` bağlam yöneticisiyle koşmalı (lifespan/`init_db` yalnızca öyle tetiklenir).
- **VideoAnalyzer uygulandı** (2026-07-09, ARCHITECTURE §3.4): `services/video/` — `FakeVideoAnalyzer` (ağa çıkmaz, sabit çıktı) + `RoboflowVideoAnalyzer` (iki hosted YOLOv8 modeli: koli/palet + hasar, düz `requests` REST — `inference-sdk` Python 3.13'ü henüz desteklemiyor). `VIDEO_PROVIDER=fake|roboflow` (default `fake`); canlı çağrı için `code/backend/.env.example`'daki `ROBOFLOW_API_KEY`'i `code/.env`'e doldur. 7 gerçek fotoğrafla doğrulandı: ayrık koli/palette güvenilir, istiflenmiş palette eksik sayıyor (bilinen sınır, §3.4'te detay). Birden fazla video/fotoğrafın toplanması (aynı teslimatın farklı çekimleri) henüz kapsam dışı — istismar riski nedeniyle üst katmanda ele alınmalı.
- **Env:** `code/.venv` (Python 3.12) — testler `cd code && ./.venv/bin/python -m pytest`. Minimal test bağımlılıkları kurulu (pydantic, openai, parser). **Canlı RAG için** `pip install -r requirements.txt` gerekir (chromadb + FlagEmbedding/torch, ~GB'lar); kurulu değilse CLI hattı RAG'ı atlayıp bağlamsız devam eder (graceful degradation). **Video için** `requests`/`opencv-python-headless` gerekir (requirements.txt'e eklendi).
- **Opsiyonel fiziksel teslimat + video takip politikası uygulandı** (2026-07-10, backend; [plans/done/opsiyonel_fiziksel_teslimat_ve_video_takip_politikasi.md](plans/done/opsiyonel_fiziksel_teslimat_ve_video_takip_politikasi.md)): fiziksel teslimat ve video artık varsayılan ödeme yolu değil, yöneticinin açtığı **takip politikası**dır. Yeni: `schemas/tracking.py`, `services/tracking_policy.py` (persistence + deterministik `recommend_physical_delivery`), `services/effective_requirements.py` (saf resolver: contractual/operational/advisory), `services/settlement.py` (tek release guard; approval, e-irsaliye ve video aynı yolu kullanır), `services/extraction_projection.py` (public cevaplarda `tax_id`/`source_quote` yok). DB: `tracking_policies` tablosu + `transactions.manager_token` (additive, idempotent migration; **eski runtime DB'ye backfill yok** — tazelemek için `code/data/runtime/m4trust.db` elle silinir). API: `manager_link` + `manager-view` + `PUT tracking-policy` + `tracking-policy/lock`; policy kilitlenmeden onay 409; `awaiting_review` onay bypass'ı **kapandı**. Kanal guard'ı: takip etkin değilse e-irsaliye/video 409, `decided` işlemde geç kanıt `TRANSACTION_DECIDED`. Semantik değişiklik: **opsiyonel video anomalisi artık `dispute_opened` üretmez**, `hold` + `manual_review_required` üretir; video sayımı `delivered_quantity` fallback'i değildir. Sözleşmesel video `document_and_video` modunu zorunlu kılar (aksi halde video yalnızca "geldi mi?" diye sayılırdı). Fake extraction fixture'ı approval-only'ye çekildi; `FakeVideoAnalyzer` ipuçları `eksik`/`hasarli`/`dusuk_guven`. Env eki: `VIDEO_ADVISORY_CONFIDENCE_THRESHOLD=0.80`. §4.2 şeması **değişmedi** (yapısal snapshot testi `tests/test_extraction_schema.py`de). **Frontend ayrı plandadır.**
- **Teslimat kanıtı yetkilendirme hotfix'i uygulandı** (2026-07-10): e-irsaliye ve video uçları query'deki seller veya manager capability token'ını doğrular; buyer/eksik/geçersiz token 403'tür ve guard kanal/state kontrolünden önce çalışır. `GET /api/transactions`, `DEMO_PUBLIC_DASHBOARD=true` olmadıkça 403 döner. SQLite bağlantıları 5 saniye `timeout` ve `busy_timeout` kullanır. Demo listesi için `code/.env` içinde `DEMO_PUBLIC_DASHBOARD=true` ayarlanır.
- **Moka M1 HTTP client uygulandı** (2026-07-11, additive yan panel; Plan 06 ile account funding/release yoluna bağlandı): `services/payments/moka/` altında CheckKey, Decimal serializer, sync HTTP client, DTO→domain mapper ve redacted trace bulunur. `PAYMENT_PROVIDER=moka_http` account funding/release için kullanılabilir; legacy approval yolu yalnız `mock` kabul eder ve uyumsuz provider'ı kontrollü reddeder. Ortam alanları `MOKA_BASE_URL`, `MOKA_DEALER_CODE`, `MOKA_USERNAME`, `MOKA_PASSWORD`, `MOKA_CARD_TOKEN`, `MOKA_SOFTWARE`, `MOKA_TIMEOUT_SECONDS`, `MOKA_CONTRACT_PROFILE`; repr/trace secret'ları maskeler. Doğrulama: `cd code && ./.venv/bin/python -m pytest` → 269 test yeşil.
- **Moka Plan 01 tamamlandı** (2026-07-11, [plans/done/01_moka_contract_mock_and_client.md](plans/done/01_moka_contract_mock_and_client.md)): M0 contract freeze + gateway/domain, M1 sync HTTP client + ayrı-process contract-faithful mock ve M1C client↔mock E2E + redacted demo driver master'dadır. Terminal 1: `cd code && ./.venv/bin/uvicorn backend.mock_moka.app:app --port 8001`; Terminal 2: `cd code && ./.venv/bin/python scripts/demo_moka_contract.py` (create→approve→detail) veya `--fault` (beklenen banka reddi). Driver `code/.env`'i okur, yoksa local mock'un demo kimliklerine düşer; stdout request/response çiftleri redacted'dir. E2E 7/7, tam paket 303/303 yeşildir. Sandbox/canlı Moka doğrulaması yapılmadı; ana payment/settlement cutover'ı Plan 06'ya kadar yoktur.
- **Plan 02 foundation tamamlandı** (2026-07-11, [plans/done/02_foundation_migration_db_api_ci.md](plans/done/02_foundation_migration_db_api_ci.md)): `db.py` geriye uyumlu `db/` paketine taşındı; atomik `001_baseline_current_schema`, fail-closed legacy fingerprint/stamping, request-scoped `get_db`, background connection ve `BEGIN IMMEDIATE` transaction helper eklendi. Router'lar ortak dependency'ye, transaction sorguları repository seam'ine geçti. `ActorContext`/access-control imzaları, standart yeni-API hata zarfı, request-id middleware, audit/state kontratları ve hafif CI profili dondu; app wiring `main.py`dedir. CI OpenCV kurmaz: gerçek frame sampling importu lazy'dir, video profili yoksa ilgili codec testi skip olur; fake/Roboflow testleri çalışır. Komut: `cd code && ./.venv/bin/python -m pytest -q`; CI: `pip install -r requirements-ci.txt && pytest -q`.
- **Plan 03 tamamlandı** (2026-07-11, [plans/done/03_identity_legal_entity_party_onboarding.md](plans/done/03_identity_legal_entity_party_onboarding.md)): identity/session/legal-entity/membership (Faz 3A) + participant/invitation/audit (Faz 3B) + transaction ownership cutover (Faz 3C). Migration `003`-`007` registry'ye kayıtlı (`002` rezerve/kullanılmamış). Yeni: `services/auth.py` (Argon2id, session-cookie `m4t_session` HttpOnly + CSRF cookie `m4t_csrf`, `X-CSRF-Token` + Origin doğrulaması, `last_seen_at` 60sn throttle), `services/identity.py` (AES-256-GCM tax ID + deterministik HMAC lookup, insecure fallback yok — `APP_ENCRYPTION_KEY`/`APP_HMAC_KEY` yoksa `KeyConfigurationError`), `services/participants.py` (frozen `ParticipantService`, v2 §8.1), `services/invitations.py`, `services/notifications.py` (`FakeNotificationProvider`), `services/audit.py` (`audit_events`, business mutation ile aynı connection). `access_control.py::get_current_actor` artık session cookie'sini (ad-hoc `connect()`, imza `(request)` frozen kaldı) capability token'dan önceliklice çözer; `X-Acting-Entity-ID` yalnız doğrulanmış aktif membership'te dolar. `POST /api/transactions` **additive dual-mode**: `acting_entity_id`+`own_role` yoksa mevcut anonim `legacy_v1` akışı değişmeden çalışır; verilirse authenticated `account_v2` akışı (capability token yok, `ParticipantService.attach_creator`+placeholder+opsiyonel invitation). List/detail `account_v2` için assignment-scoped, `legacy_v1` açık kaldı. Detay cevabına `lifecycle_version`+`canonical_state` (§5.3 projeksiyonu) eklendi. `LEGACY_CAPABILITY_ACCESS_ENABLED` artık varsayılan `false`'tur; party/manager-view/delivery/evidence/approvals legacy capability yüzeyinin tamamı flag arkasındadır; dar `legacy_compat` test seti env ile açar. **Tasarım sapması:** Plan dokümanının "anonim create 401" ifadesi literal/global uygulanmadı (8 legacy senaryo test dosyasını koordinasyonsuz kırardı); bunun yerine yalnız yeni account-mode çağrı biçimi (yeni alanlarla) auth ister — additive-first ilkesi (v2 §2.2/§15.4) esas alındı, gerekçe PR #28'de. `main.py`'ye `auth`/`entities`/`participants`/`invitations` router'ları bu kapanış commit'inde eklendi. Yeni bağımlılıklar: `argon2-cffi`, `cryptography` (`requirements-core.txt`, CI'a `requirements-ci.txt` üzerinden geçer). Demo seed: `scripts/seed_demo_users.py` (idempotent, Berke/Yusuf + ABC A.Ş./XYZ Ltd.). Doğrulama: `cd code && ./.venv/bin/python -m pytest -q` → 567 test yeşil (Plan 02 sonrası baseline 415'ten).
- **Plan 03 kabul remediation'ı** (2026-07-11): anonim `legacy_v1` upload korunurken account create ve tüm invitation/participant mutation'ları CSRF guard'ına alındı. Aynı role yeni invitation eski pending'i revoke/supersede eder; bound role daveti reddeder; accept participant'ı atomik compare-and-set ile bağlar ve diğer pending davetleri revoke eder. Audit metadata yalnız scalar enum/ID/status taşır; hassas veya serbest metin değerleri güvenli anahtar altında da reddedilir. Migration `005` değiştirilmedi, yeni migration gerekmedi.
- **Plan 04 Wave A tamamlandı** (2026-07-11, `integration/plan-04-wave-a-close`): 4A document provenance/extraction runs/immutable rule versions ile 4B review/reconciliation çekirdeği gerçek uygulamaya bağlandı. Migration registry `001,003-010`; `reviews.router` `main.py`de. Account pipeline validator `NEEDS_REVIEW` sonucunda blocking `validator` case açar; participant confirm current rule-set ile extracted↔confirmed reconciliation yapıp güvenli reason-code'lu `party_mismatch` case açar. `approvals.py` ve `evidence.py`, account için `rule_set_versions`, legacy için `extracted_rules` fallback kullanan `repositories/rule_sets.py::get_current` seam'indedir. `ReviewService` ve `RuleVersionService` imzaları donuk kaldı; `ExtractionJSON` değişmedi. Baseline 706; kapanış full suite 710 test yeşil. **Plan 04 bütünü kapanmadı; Wave B sıradadır.**
- **Plan 04 Faz 4C/4D tamamlandı** (2026-07-11, `feat/ratification-package-canonicalization`, base `3dd299d`): 4C compiler freeze korunarak migration `011_ratification_packages`, canonical `RatificationPackageService`, immutable package repository/schema, account_v2 CAS lifecycle helper ve provider çağırmayan `FundingCoordinator.ensure_pool_funded` eklendi. Package readiness document/rule/confirmed buyer+seller/locked policy/funding schedule/blocking review koşullarını fail closed denetler; input değişimi supersede + yeni version üretir. `funding_pending` yalnız complete current package için idempotent `funding_required` event/audit ile üretilir; gerçek provider funding Plan 06’dadır. Full suite 736 test yeşil; 4E/4F henüz bekliyor.
- **Plan 04 Faz 4E/4F-1 ilerledi** (2026-07-11, integration `program/domain-evolution-v2` @ `b5f5920`): Yusuf’un account ratification API + `012_ratifications` dosyası ve legacy approval cutover’ı merge edildi; `012` registry/ratifications app wiring’i Plan 04 kapanışına bırakıldı. Berke’nin `feat/rule-revision-endpoints` işi tam `ExtractionJSON` revision/revalidation uçlarını, creator-side manager/entity authorization’ı, current-parent CAS/stale guard’ı, otomatik validator + NEEDS_REVIEW case hook’unu, safe rule events/audit’i ve pre-funding package supersede/invalidation’ını ekledi. `ExtractionJSON` değişmedi; 4F-2 ve kapanış entegrasyonu bekliyor.
- **Plan 04 kapanışı tamamlandı** (2026-07-11, `integration/plan-04-close`): Kullanıcı tarafından doğrulanan 4F-2 merge’i üzerine `012_ratifications` migration registry/alias’ı ve `ratifications.router` gerçek app wiring’i tamamlandı; gerçek app ratification → aynı package hash → çift ratification → `funding_pending`/tek `funding_required` gate’i eklendi. Plan 04 ready→done taşındı; 05 sıradaki plandır. Kapanış full suite: **765 test yeşil, 30 warning**.
- **Plan 06 tamamlandı** (2026-07-12, `integration/plan-06-close`): Milestone/funding-unit persistence, gerçek pool funding ve account settlement cutover. **6A** — migration `015`-`017` runtime registry'sine bağlandı (önce dosya vardı ama registry dışıydı → Plan 04 fallback koşuyordu); `FundingCoordinator.ensure_pool_funded` artık her funding unit için `create_pool_payment` yapar, tümü pool_created olunca transaction `active` (funding exactly-once; payload-faithful schedule materialization, drift fail-closed; timeout→`pool_creation_unknown`→detail reconcile). **6B** — saf `services/milestone_decision.py` evaluator (DB/HTTP-bağımsız); trigger gating: desteklenmeyen trigger fail-closed hold, trigger gerçekleşmeden unit eligible olmaz. **6C** — `services/settlement.py` account yolu funding-unit release'e cutover (legacy_v1 bit-bit korunur); `services/payments/release_coordinator.py` idempotent instruction → `approve_pool_payment(identifier)` (amount YOK) → detail reconcile → unit approved → milestone aggregate → `settled`. Bir funding unit = bir pool payment = tek bütün approve; `capture_ratio` account yolundan kalktı; fixed-tranche %50 = eşiği geçen unit'lerin ayrı approve'u. **6D** — mock Moka HTTP üzerinden multi-release E2E. **Closure** — evidence submit uçları account settlement'ı tetikler (tek release guard); video advisory `review_required` video da okur ve `matched_box` alanını kullanır (yüksek güvenli hasar release'i durdurur); kümülatif teslim = verified e-irsaliye delta'larının toplamı; `LEGACY_CAPABILITY_ACCESS_ENABLED` default **false** (env ile açılır; legacy testler dar `legacy_compat` pytest marker'ıyla flag'i açar). **Migration istisnası:** 6A, merge edilmiş `015`/`017` dosyalarını registry'ye bağlarken düzeltti — bu tek seferlik güvenli istisnadır çünkü 015-017 daha önce hiçbir registry/DB'ye uygulanmamıştı (fallback yolu kullanılıyordu); bundan sonra merge edilmiş migration değiştirilmez, düzeltme yeni migration ile gelir. Full suite **909 test yeşil**.
- **Plan 05 kapanış entegrasyonu** (2026-07-12 remediation dahil, `integration/plan-05-close`): `013_evidence_records` + `014_disputes` startup migration registry'ye alındı; corrective `023_plan05_remediation_constraints` provenance immutability ve `review_actions.escalate_dispute` constraint'ini ekledi. `evidence_submit` ve `disputes` router'ları `main.py`'ye bağlandı. Account evidence yalnız `active` state'te kabul edilir; account settlement Plan 05'te `active` state'ini korur. Video exact replay hash-first/deterministic storage kullanır; analyzer kalıcı storage'dan önce çalışır ve concurrent analyzer hatası başarılı upload dosyasını silemez. Dispute action matrisi comment/attach=iki taraf approver'ı, cancel=opener, resolve=opener veya platform reviewer/admin'dir. Review `escalate_dispute` yalnız buyer/seller participant approver'ın explicit aksiyonuyla yeni dispute açar; platform reviewer/admin review state'ini yönetir. 5C bundle GET saf okuma, snapshot POST explicit mutation olarak korunur. 015-017 artık Plan 06 startup registry'sindedir.
- **Plan 06 + 06X kapanışı** (2026-07-12): 015-017 funding-unit persistence, gerçek pool funding, account settlement/release coordinator, milestone-scoped evidence, review/dispute sonrası idempotent settlement re-trigger, legacy approval/tracking-policy kill-switch ve reconciliation provider binding'i tamamlandı. `LEGACY_CAPABILITY_ACCESS_ENABLED` default `false`'tur; account evidence `milestone_id` ile tek adayda bağlanır, çoklu adayda 409 verir; e-irsaliye HTTP body'sindeki açık `milestone_id` servise aktarılır; video advisory miktar üretmez. Plan 06 ve 06X dosyaları `plans/done/` altındadır. Full suite: **913 passed, 45 warnings**.
- **Plan 07 tamamlandı** (2026-07-12, `integration/plan-07-close`, temel: `feat/payment-reconciliation-jobs` PR [#61](https://github.com/gencberke/M4Trust-B2B-deal-management/pull/61) + `feat/payment-failure-review-and-faults` PR [#60](https://github.com/gencberke/M4Trust-B2B-deal-management/pull/60) + `integration/plan07-review-action-wiring` PR [#62](https://github.com/gencberke/M4Trust-B2B-deal-management/pull/62)): funding-unit reconciliation (`services/payments/reconciliation.py`, pool/approved/refunded/not-found/drift matrisi), güvenli release retry (reconciliation-first, kör retry yok, aynı idempotency key), insan kontrollü undo/refund (`payment_resolutions`/`payment_resolution_approvals` — manager yalnız talep açar, execution yalnız platform reviewer/admin veya bilateral buyer+seller onayı), `processing_jobs` state machine + startup recovery (extraction/funding/release/reconcile job kaydı, `last_error_code` her zaman güvenli reason-code), redaction'lı `GET .../payment-trace`. Migration `018_processing_jobs` + `024_payment_lifecycle_operational_extensions` (016/017/023 dosyaları değiştirilmedi; additive CHECK genişlemesi rebuild-and-copy ile yapıldı). Yusuf'un 7B'si review reason-code/resolution-code kontratını (`PAYMENT_POOL_CREATION_FAILED`/`APPROVE_FAILED`/`RECONCILE_AMBIGUOUS`/`UNDO_BLOCKED`/`REFUND_FAILED`, `open_payment_review_case` tek açılış kapısı) dondurdu ve mock Moka fault matrisini genişletti (`DEMO-TOKEN-TIMEOUT-AFTER-CREATE`, approve-timeout, `statement_closed`); Faz 7A bunu tüketti (Berke'nin ilk implementasyonunda ad-hoc `PAYMENT_REFUND_UNSUPPORTED` reason-code'u ve yanlış `source_id` review kapanışta `PAYMENT_REFUND_FAILED` + `funding_unit_id`'ye düzeltildi). Yusuf'un takip PR'ı (`§8`), `review.py::record_action`'daki `resolve_continue`'u payment operation sonucuna bağladı (case yalnız gerçek provider/reconciliation sonucu definitif olduğunda kapanır). Refund gerçek Moka contract'ı hâlâ yok; `ports.py::RefundCapableGateway` opsiyonel seam'dir, yalnız `FakePaymentGateway` implement eder — gerçek gateway'de yoksa fail-closed review + provider side-effect yok. **Bulunan bug:** `approve_resolution` audit metadata anahtarı `"participant_role"`, güvenlik filtresindeki `"pan"` substring taramasına (kart PAN'i için) yanlışlıkla takılıp her çağrıda crash ediyordu — bilateral onay akışı hiç test edilmediği için fark edilmemişti; `"approver_role"`e çevrildi. Plan 07 dosyası `plans/done/` altındadır. Full suite: **956 passed, 49 warnings**.
- **Plan 07 review remediation** (2026-07-12, `fix/plan07-reversal-correctness`): Master-merge öncesi dış review'da finansal reversal yolunda 3 blocker + 2 major bulundu ve `execute_resolution` (`services/payments/payment_operations.py`) yeniden tasarlandı. **BOLA düzeltmesi:** `_can_execute` artık bilateral approval tamamlanmış olsa bile tetikleyen aktörün bu transaction'la ilişkili olmasını (`platform reviewer/admin` veya `manager` veya aktif `buyer/seller` approver) zorunlu kılar — opak `resolution_id`'ye erişen ilgisiz authenticated kullanıcı artık reversal tetikleyemez. **Atomik claim:** `payment_resolutions.claim_executing()` (`UPDATE ... WHERE status IN (...)`, CAS) provider çağrısından önce `executing`e geçer; concurrent ikinci çağrı kör biçimde beklemez/tekrar provider çağırmaz, mevcut/son durumu yorumlar. **Unknown reversal artık kurtarılabilir:** instruction provider çağrısından ÖNCE oluşturulur (get-or-create, kaybolmaz); `execute_resolution` bir `unknown` resolution'a tekrar çağrılınca (örn. `resolve_continue` wiring üzerinden) provider detail'iyle reconcile eder — detail definitif olarak reversal'ı doğruluyorsa provider'a tekrar gitmeden `executed` yapar, hâlâ `APPROVED` gösteriyorsa (reversal hiç işlenmemiş) kontrollü/informed retry'a düşer, aksi hâlde `unknown` kalır. **Lifecycle drift artık yutulmaz:** `_transition_after_undo`/`_transition_after_refund` artık `bool` döner (yalnız `AccountLifecycleError` yakalanır, bare `except Exception` kaldırıldı); local provider/unit state HER ZAMAN güncellenir (provider side effect geri alınamaz) ama lifecycle geçişi başarısız olursa resolution `executed` YAPILMAZ — `unknown`da kalır, case açık kalır, API tam başarı bildirmez. **Retry artık review case'i kapatıyor:** `retry_release_instruction` başarılı olduğunda ilgili `PAYMENT_APPROVE_FAILED` case'i `review_service.resolve_case(resolution_code=RETRY_PAYMENT_AUTHORIZED)` ile çözer (önceden sonsuza kadar açık kalıyordu). 6 yeni regresyon testi eklendi (BOLA 403/409, concurrent claim, undo/refund unknown→executed reconciliation, lifecycle drift, retry case closure). **Bilinçli kapsam dışı bırakılan (Major, blocker değil):** processing-job dispatcher'ı hâlâ yok — `reconcile`/`funding`/`release` kind'leri mevcut `POST /payments/reconcile` ve `POST /release-instructions/{id}/retry` uçlarıyla operatör-tetiklemeli kurtarılabilir, ama `extraction` kind'i (stuck `extracting` transaction) için hiçbir re-trigger ucu yok — bu açık risk olarak bırakıldı, gerçek bir dispatcher ayrı bir iştir. Full suite: **962 passed, 49 warnings**.
- **Plan 07 extraction job recovery** (2026-07-12, `fix/plan07-extraction-job-recovery`): Önceki remediation'da bilinçli olarak açık bırakılan tek gerçek dispatcher boşluğu (stuck `extracting` account_v2 transaction'lar için re-trigger ucu yokluğu) kapatıldı — genel amaçlı worker/scheduler EKLENMEDİ, yalnız dar bir explicit retry seam'i: `POST /api/transactions/{id}/extraction/retry` (`routers/extraction_ops.py` + `services/extraction_recovery.py`). Yalnız `account_v2` + (transaction `extracting` VEYA job `queued|retry_pending|failed|unknown`) durumunda kabul edilir; manager veya platform reviewer/admin (`review_service.is_transaction_manager`/`is_platform_reviewer_or_admin` reused). Atomik claim `processing_jobs.claim_for_retry()` (`payment_resolutions.claim_executing` ile aynı CAS deseni, `UPDATE ... WHERE status IN (...)` + `attempt_count+1`) — yalnız claim'i kazanan çağrı `transaction_pipeline.run_pipeline`'ı (mevcut yol, mantık kopyalanmadı) senkron çağırır; kaybeden `409 EXTRACTION_RETRY_IN_PROGRESS` alır, provider'a hiç gitmez. Mevcut kalıcı `contract_documents`/storage referansından `AccountPipelineInput` yeniden kurulur (orijinal `original_filename`'den suffix/passthrough türetilir) — yeni upload yok. Startup hook'a (`main.py::_recover_operational_jobs`) dokunulmadı, hâlâ yalnız işaretler/provider çağırmaz; gerçek çalıştırma yalnız bu explicit uçtan. 12 regresyon testi (`test_extraction_job_recovery.py`) — kritik bulgu: zaten BAŞARIYLA tamamlanmış (rule_set_versions oluşmuş) bir transaction'ı yeniden extraction'a almak `UNIQUE(transaction_id, version)` ihlaline düşer; bu retry'ın kapsamı DIŞINDADIR (rule-revision uçlarının işi) — testler bu yüzden gerçek crash'i (`run_pipeline` çağrısını process-ölümü noktasında kesen bir stub ile) simüle eder, zaten-tamamlanmış bir transaction'ı değil. Full suite: **974 passed, 49 warnings**.
- **Plan 09 privacy/provenance/readiness tamamlandı** (2026-07-12): encrypted raw/markdown/evidence storage, bounded upload, retention + explicit legacy migration, verified backup/restore, immutable tracking-policy versions, structured allowlist JSON logs, rate-limit/lockout/reset/verification, fake+SMTP notification adapters, OCR/LLM/RAG/analyzer provenance, acting-entity IDOR hardening, migrations `019-022` + corrective `025`, dependency/static/secret CI scans ve PostgreSQL-readiness envanteri eklendi. Legacy davranış silinmedi; default kapalı removal checklist'i ayrı planning planındadır. Yetkili account çağrıları explicit ve membership-validated `X-Acting-Entity-ID` ister. Operasyon/security belgeleri `docs/` altındadır.
